/**
Client SuiteScript on Work Order FieldChange to update DueDate based on StartDate

**/

function ValidateDates(type, name) {
	if (name == 'custrecord_wfmworkorder_startdate') {
		alert('Hello, World!');
	}
	if (name == 'Start Date') {
		alert(name);
	}
}